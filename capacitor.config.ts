import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.frelian.app.dvp.store',
  appName: 'doublev-ionic-store',
  webDir: 'dist'
};

export default config;
